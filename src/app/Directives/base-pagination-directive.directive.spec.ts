import { BasePaginationDirectiveDirective } from './base-pagination-directive.directive';

describe('BasePaginationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BasePaginationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
